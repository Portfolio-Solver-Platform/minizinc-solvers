# minizinc-solvers
Can generate data for all solvers minizinc has to offer 

## Setup
Install Docker, then run `nix develop`.

## Updating dependencies
You can manually update dependencies by:
```bash
uv sync
```
